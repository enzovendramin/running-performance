# Copy this file to .env and fill in your real values.
# .env is NEVER committed (it's in .gitignore). This template carries no secrets
# and stays in the repository.

# --- Garmin Connect ---
# Your Garmin Connect account login (an account without 2FA).
GARMIN_EMAIL=your-email@example.com
GARMIN_PASSWORD=your-password
